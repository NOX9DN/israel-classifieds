/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
};

export default nextConfig;
```

Сохрани **(Ctrl+S)** потом в PowerShell:
```
git add .
git commit -m "fix next config"
git push